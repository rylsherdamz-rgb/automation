[project]
name = "faceless-youtube-automation"
version = "0.1.0"
description = "Faceless YouTube automation: AI video generator + long-video clipper with a tkinter GUI"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
]

[tool.uv]
package = false